export const metadata = {
  title: 'Workspace Intelligence — AGI-1',
  description: 'AGI-1 Workspace Intelligence command center for Google Workspace operations',
};

export default function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
